import { createTheme } from '@mui/material';

const paletteTheme = createTheme({
  palette: {
    primary: {
      light: 'hsla(266, 41%, 40%, 1)',
      main: 'hsla(266, 41%, 28%, 1)',
      dark: 'hsla(266, 41%, 15%, 1)',
    },
  },
});

export default paletteTheme;
